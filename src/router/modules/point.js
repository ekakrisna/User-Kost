import Datatable from '@views/main/point/Datatable';
import store from '@/store';

const pointRoutes = [
  {
    path: 'point',
    name: 'point',
    component: Datatable,
  },
];

export default pointRoutes;
